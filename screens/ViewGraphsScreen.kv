#:kivy 2.1.0
<ViewGraphsScreen>:
   name: "ViewGraphsMenu"
   MDBoxLayout:
      orientation: 'vertical'
      MDLabel:
         text: 'View Graphs'
         halign: 'center'
      MDLabel:
         text: 'Add option to group by time periodes (i.e. per week, per month, per year)'
         halign: 'center'